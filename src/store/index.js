import { createStore, createLogger } from 'vuex'

// import Home from '../Modules/Home/store/Home.store'

export default createStore({
	modules: {
		// Home,

	},
	state () {
		return {
			
		}
	},
	mutations: {
		
	},
	actions: {
		// async app_init ({ dispatch, commit, rootGetters }) {
		// 	try {

		// 	} catch (e) {

		// 	} finally {
		// 	}
		// }
	},
	getters: {
		// reloadKey: (state) => state.reloadKey
	}
})